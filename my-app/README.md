React + Netlify Functions example.

Run locally:

1. Install dependencies

```
npm install
```

2. Start Netlify Dev (serves frontend + functions)

```
npm run netlify
```

Build for production:

```
npm run build
```

Deploy to Netlify: push to GitHub and import the repo in Netlify.
